from mymath import add as a, product as p

a(100, 200)  # The Sum: 300
p(10, 20)  # The Product: 200

# Note:
# Once we defined as alias name, we should use alias name only and we should not use original name
